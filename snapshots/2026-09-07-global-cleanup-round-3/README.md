# Global skill cleanup round 3

User-approved removal of 20 skill names, 40 global entry paths. Contents are materialized; manifest records original paths and links. Runtime dependencies, caches and Git metadata are excluded. Restore content first, then original links. Methodology plugins are unchanged.
